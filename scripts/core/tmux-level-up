#!/bin/bash
# tmux-level-up — Go up one level (9→8→7...→1→9)
set -euo pipefail
source "$HOME/.local/bin/tmux-multilevel/lib.sh"

current=$(active_level)
exec "$DMT_DIR/core/tmux-goto-level" $(( ((current - 2 + 9) % 9) + 1 ))
