#!/bin/bash
# tmux-level-down — Go down one level (1→2→3...→9→1)
set -euo pipefail
source "$HOME/.local/bin/tmux-multilevel/lib.sh"

current=$(active_level)
exec "$DMT_DIR/core/tmux-goto-level" $(( (current % 9) + 1 ))
